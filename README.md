# 世界の鳥あてクイズ

世界の鳥の写真を1枚見て、その種類を**10個の選択肢**から当てるクイズサイト。
ビルド不要の静的サイト（HTML / CSS / 素の JavaScript）。

## 使い方

`index.html` をブラウザで開くだけ。ローカルサーバを立てるなら:

```
python3 -m http.server 8000
# → http://localhost:8000
```

配布・共有には `dist/bird-quiz.html` を使う。CSS と JS を埋め込んだ1ファイル版で、
ダブルクリックするだけで動く。`js/data.js` などを編集したら作り直すこと:

```
node build-standalone.js
```

出題数は 10問 / 20問 / 全81問 から選べる。特定のグループに偏らないよう、
グループごとに1種ずつ取っていく順で候補を作ってからシャッフルしている。

キーボードでも解答できる（`1`〜`9` と `0` が10個の選択肢に対応、`Enter` で次へ）。
右上のボタンでライト／ダーク配色を切り替え（設定は localStorage に保存）。

## 選択肢のつくり方

**選択肢は設問ごとに組み直される。** 同じ鳥が出ても、並ぶ名前は毎回変わる。

1問あたり「正解1 + ダミー9」の計10個。ダミーはまず**同じグループから3〜5種**を取り、
残りを他のグループから埋める（`js/app.js` の `buildChoices`）。

こうしているのは、全部ランダムだと「ペンギンの写真に対して唯一のペンギン」のような
一目瞭然の問題になりやすく、逆に全部同じグループだと難しすぎるため。
実測でダミー9個のうち平均約3.9個が同じグループになる。

## 出題される鳥（全81種 / 11グループ）

| グループ | 種数 |
| --- | --- |
| 走鳥類 | 5 |
| 地上を歩く鳥 | 8 |
| カモ・ハクチョウの仲間 | 8 |
| ペンギン | 5 |
| 海鳥 | 7 |
| 水辺の大型鳥 | 11 |
| 猛禽類 | 9 |
| フクロウ類 | 4 |
| インコ・オウム類 | 6 |
| 森の色鳥 | 11 |
| スズメ目の鳥 | 7 |

グループは選択肢を作るための「似た仲間」のゆるい括りで、分類学上の区分とは一致しない
（例: カンムリバトはハト目だが「地上を歩く鳥」に入れてある）。
正確な目・科は解答後の解説に `taxon` として表示される。

## 写真について

写真はリポジトリに同梱していない。各種に紐づけた Wikipedia 記事の代表画像を、
出題時に Wikipedia の REST API（`/api/rest_v1/page/summary/`）から取得している。
日本語版で見つからなければ英語版を試し、どちらも取れなければ
「写真を読み込めませんでした」と表示して、その問題はそのまま進む。
取得した URL はセッション中 `sessionStorage` にキャッシュし、次の問題の画像は先読みする。

そのため、**初回の表示にはネットワーク接続が必要**。著作権は各撮影者に帰属する。

### 特定の種だけ写真が出ないとき

`wiki.ja` の記事名が実際の Wikipedia の項目名とずれている可能性が高い。
`js/data.js` の該当する種の `wiki.ja` / `wiki.en` を正しい記事名に直すか、
`image` に画像の URL / 相対パスを直接書けば解決する。
`image` を書いた種は Wikipedia への問い合わせを行わない。

```js
{
  id: 'shoebill', group: 'wader',
  image: 'images/shoebill.jpg',       // ← ここを書くだけ
  name: 'ハシビロコウ', en: 'Shoebill', sci: 'Balaeniceps rex',
  ...
}
```

すべての種に `image` を入れれば、完全にオフラインで動く。

## 公開（GitHub Pages）

`.github/workflows/pages.yml` がリポジトリの中身をそのまま GitHub Pages にデプロイする。
公開URLは:

```
https://roomhair.github.io/bird-quiz/
```

公開するには、先に次の2つをリポジトリの設定画面で済ませておく必要がある。

1. **Settings → General → Danger Zone → Change visibility** で public にする
   （private リポジトリからの Pages 公開は有料プラン限定のため）
2. **Settings → Pages → Build and deployment → Source** を **GitHub Actions** にする

ワークフローは `build`（全ブランチ）と `deploy`（`main` のみ）に分かれている。
Pages 環境は既定でデフォルトブランチからのデプロイしか許可しないため、
実際に公開するには変更を `main` にマージする。

ワークフローは、`dist/bird-quiz.html` が最新のソースから作り直された状態かどうかも
確認する。`js/data.js` などを編集したら `node build-standalone.js` を実行してコミットすること。

## 鳥を追加・編集する

`js/data.js` の `BIRDS` に要素を足すだけ。1種がそのまま1問になり、同時に他の問題の
選択肢の候補にもなる。

| フィールド | 内容 |
| --- | --- |
| `id` | 一意な識別子（画像キャッシュのキーにも使う） |
| `group` | `GROUPS` の `id`。同じ値の種が選択肢に優先的に混ざる |
| `name` / `en` / `sci` | 和名 / 英名 / 学名 |
| `taxon` | 目・科（解答後に表示） |
| `region` / `size` | 分布 / 大きさ |
| `image` | 画像の URL / 相対パス。空なら Wikipedia から取得 |
| `wiki.ja` / `wiki.en` | 画像取得に使う Wikipedia の記事名 |
| `note` | 解答後に表示する一言解説 |

グループを増やす場合は `GROUPS` に足す。スタート画面の並び順はこの配列の順どおり。
1グループの種数が10を大きく下回っても選択肢は他グループから補われるので問題ないが、
極端に少ないグループを作ると「同じグループのダミー」が機能しにくくなる。

選択肢の数を変えたいときは `js/app.js` の `CHOICE_COUNT` と、
それに対応するキーボードショートカット `KEYS` を合わせて変更する。

## ファイル構成

```
index.html      3画面（スタート / 出題 / 結果）のマークアップ
css/style.css   配色トークン、レイアウト、ライト・ダーク両対応
js/data.js      11グループ81種の鳥のデータ
js/app.js       出題ロジック、選択肢の生成、画像取得、採点、結果表示
images/         自前の写真を置く場所（初期状態では空）
dist/           1ファイル版の出力先
build-standalone.js  css/js を index.html に埋め込んで dist/ に書き出す
.github/workflows/pages.yml  GitHub Pages への自動デプロイ
```
